node_modules/
.next/
out/
build/
.env
.env.local
.env*.local

# Temporary DO app-spec dumps used by doctl apps update — contains plaintext secrets.
.do-spec*.yaml
*.log
.DS_Store
.vercel
.turbo
next-env.d.ts.bak
.idea/
.vscode/

# Claude Code: machine-local permission allowlist + prefs (the .local suffix
# means "do not commit"). Project-shared agent / hook configs would live in
# .claude/settings.json (no .local) and stay tracked.
.claude/settings.local.json

package-lock.json
